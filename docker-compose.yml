version: '3'

services:
  postgres:
    image: postgres:13
    volumes:
      - postgres-dma:/var/lib/postgresql/data
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_PASSWORD=mysecretpassword


volumes:
  postgres-dma:
    external: true